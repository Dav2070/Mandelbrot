<Page
    x:Class="Mandelbrot.MapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mandelbrot"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Name="ContentRoot" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Name="OptionsStackPanel" Grid.Row="0" Orientation="Horizontal">
            <TextBox Name="GenauigkeitTextBox" Text="0,1" PlaceholderText="Genauigkeit" InputScope="AlphanumericFullWidth" />
            <TextBox Name="FaktorTextBox" Text="200" PlaceholderText="Faktor" InputScope="AlphanumericFullWidth" />
            <Button Name="ZeichnenButton" Content="Zeichnen" Click="ZeichnenButton_Click" />
            <TextBlock Name="ProgressTextBlock" />
        </StackPanel>

        <StackPanel Name="MapStackPanel" Grid.Row="1" />
    </Grid>
</Page>
